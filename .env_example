# .env_example — 이 파일을 .env로 복사 후 값을 채워주세요
# cp .env_example .env

# Picovoice (Wake Word)
PICOVOICE_ACCESS_KEY=your_picovoice_access_key_here

# 마이크 디바이스 번호 (arecord -l 로 확인)
MIC_DEVICE=0

# MySQL 데이터베이스
DB_USER=your_db_user
DB_PASSWORD=your_db_password
